---
applications:
  - name: appstatusobsdashboard
    memory: 512M
    instances: 2
    buildpack: python_buildpack
    command: uvicorn main:app --host 0.0.0.0 --port 8080
    health-check-type: http
    health-check-http-endpoint: /api/health-summary
